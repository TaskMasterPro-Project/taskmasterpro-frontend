import { Card, Container } from "@mui/material";
import { Box } from '@mui/system'
import React from 'react'

type Props = {}

function ProjectPage({}: Props) {
  return (
      <Box>
          <Card sx={{padding: "50px 50px"}}>ProjectPage</Card>
      </Box>
  );
}

export default ProjectPage